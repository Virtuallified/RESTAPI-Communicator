# RESTAPI-Communicator
A small VB.Net application for testing the GET, POST and other HTTP Methods. It's now configured with online mock Rest API's but one can modify it for online/offline(local) API's.
